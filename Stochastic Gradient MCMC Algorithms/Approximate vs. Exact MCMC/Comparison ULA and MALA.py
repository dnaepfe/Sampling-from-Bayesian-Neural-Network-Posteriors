import numpy as np
import matplotlib.pyplot as plt



def ULA(theta_0,step_size,num_samples,grad_log):
    
    theta = np.array(theta_0, dtype=float).reshape(-1)
    d = x.size
    samples = np.emtpy((num_samples,d))
    samples[0] = theta

    for k in range(0,num_samples-1):
        


    return samples

def MALA():